\documentclass[12pt,a4paper]{article}

% --- Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{adjustbox}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[protrusion=true,expansion=false]{microtype}
\setlength{\emergencystretch}{3em}
\usepackage{url}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true, linkcolor=blue, filecolor=magenta, urlcolor=cyan,
    pdftitle={A Comparative Analysis of Relational Database Access-Layer Performance in Express.js across PostgreSQL and MySQL},
}
\usepackage{csquotes}

% --- Bibliography (biblatex + biber) ---
\usepackage[backend=biber, style=ieee, sorting=nyt]{biblatex}
\addbibresource{references.bib}

% --- Document metadata ---
\title{A Comparative Analysis of Relational Database Access-Layer\\
  Performance in Express.js across PostgreSQL and MySQL}
\author{Mateusz Miotk\\
  Faculty of Mathematics, Physics and Informatics,\\
  University of Gdańsk\\
  \texttt{mateusz.miotk@ug.edu.pl}}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Node.js/Express applications reach relational databases through a spectrum of
access layers: native drivers, query builders, and object-relational mappers
(ORMs). Practitioners choose between them largely on the basis of vendor
benchmarks, which are fragmented, PostgreSQL-only, and report throughput or
latency but rarely the tail (p99). We present a neutral, reproducible benchmark of
the full access-layer taxonomy (native drivers \texttt{pg} and \texttt{mysql2}, a
query builder Knex, and six ORMs: Drizzle, Prisma, Sequelize, TypeORM, Objection,
and MikroORM), served through an identical Express application over five access
patterns, on both PostgreSQL and MySQL, reporting throughput and tail latency
(p99) together. Abstraction cost concentrates in relation-materializing
patterns: the deep/nested fetch spreads up to $6.2\times$ between the native driver
and the heaviest data-mapper ORM, while raw-SQL aggregation is much less sensitive
and point reads only mildly affected; the coarse ordering is stable across engines
and writes are engine-bound.
\end{abstract}

% \tabledir is set here so the shared results section can locate the generated
% tables; the IST build (paper/ist/) sets it to ../tables/ instead.
\newcommand{\tabledir}{tables/}

\input{sections/introduction}
\input{sections/related_work}
\input{sections/methodology}
\input{sections/results}
\input{sections/discussion}
\input{sections/threats}
\input{sections/conclusion}

\printbibliography

\end{document}
