\section{Conclusion and Future Work}
\label{sec:conclusion}

This paper's contribution is a \textbf{comparability protocol} that turns access-layer
benchmarking into a controlled experiment rather than vendor-style number-reporting: it
admits a layer's timing only once that layer returns byte-identical results (semantic
equivalence), pairs a documentation-selected estimand with a same-SQL standardized contrast
(strategy standardization), and separates capacity, tail-under-demand, and matched-utilization
latency (operating-point separation). An open, reusable harness operationalizes it,
demonstrated by a dual-engine case study --- eleven implementations across PostgreSQL and
MySQL over five access patterns (Section~\ref{sec:introduction}).

Across the five tested patterns, the documentation-selected implementation-and-strategy difference
is largest on the deep fetch that assembles a nested object graph and is smaller on point reads and
aggregation. Among the raw
paths running common SQL the spread is far smaller --- a standardized diagnostic, not a bound
(Section~\ref{sec:results}).

On the pinned compatible stable library versions no single server process uses more than about 1.1 application cores,
while adding workers increases aggregate throughput at additional resource cost. The native
implementation leads each of the ten engine--pattern comparisons; read orderings are similar between the tested backend stacks while aggregation and insert orderings reverse (Prisma drops from mid-pack to last on
the MySQL insert), cautioning against generalizing single-engine results. Rankings are also
version-sensitive: an earlier headline did not survive re-running on newer releases
(Section~\ref{sec:discussion}).

Development also surfaced a reusable checklist of genre-specific benchmarking pitfalls --- one,
a fast-but-wrong write path that passed the read-level cross-check, added by this very re-run
(Section~\ref{sec:discussion}).

These findings held under a concurrency sweep (1 to 256 connections), resting on large
between-layer effect sizes with non-overlapping bootstrap intervals, the descriptive
adjacent-rank permutation tests agreeing (Section~\ref{sec:results}).

Extensions would deepen the evidence --- a two-machine cross-run to bound observer
interference beyond the open-loop check, more engines and dataset sizes, and a dedicated study
of the N+1 penalty and cold-start latency --- and the released harness, re-run against
Prisma~7, makes these straightforward, addressing the repeatability gap documented in
computer-systems research~\cite{collberg2016repeatability}.
