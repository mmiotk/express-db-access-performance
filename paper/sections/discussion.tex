\section{Discussion}
\label{sec:discussion}

\paragraph{Implications for practice.}
Read together, RQ1 and RQ3 point to the same conclusion: abstraction cost is not
a fixed, uniform overhead but is specific to the \emph{access pattern},
concentrating where the layer must hydrate rows into objects and resolve
associations rather than merely issue SQL (the object-relational impedance
mismatch~\cite{ireland2009impedance} made concrete); this is where the
data-mapper ORMs, Sequelize, TypeORM, Objection, and especially MikroORM, trail
the native driver by the widest spreads in the study (Section~\ref{sec:results}).
A team whose hot path assembles nested object graphs therefore pays the largest
abstraction cost on its highest-value traffic. Point reads and aggregation are
the opposite case, dominated by connection and parsing cost every layer shares,
spreading little once every adapter, native drivers included, runs the same raw
plan. The same-SQL control (Section~\ref{sec:results}) shows this cost is
bounded rather than localized to any one mechanism, since it changes each
layer's eager-loading strategy, query count, wire protocol, statement
preparation, and object hydration together. Round-trip count alone does not
set throughput: Prisma and Objection both issue four deep-fetch queries
(Supplement Table~S2), yet sit at opposite ends of the default-configuration
ranking, while Knex and Drizzle sit in a consistent middle ground on every
pattern, which is what a query builder and a lightweight ORM should deliver.

\paragraph{Same SQL, different wire protocols.}
Server-side capture confirms the same-SQL collapse is not a protocol artifact
(Section~\ref{sec:results}): Prisma leads the same-SQL cell on MySQL using the
binary protocol, yet \texttt{mysql2-tuned}'s binary path otherwise sits within
noise of plain \texttt{mysql2}'s text path, so protocol choice alone does not
explain the spread.

\paragraph{The CPU cost behind parity.}
Prisma shows that an ORM need not be slow, consistent with evidence that ORM
cost is highly sensitive to implementation and tuning~\cite{vanzyl2009optimisations}:
it matches or leads the native driver on several patterns
(Section~\ref{sec:results}), but at roughly five times the application-side
CPU of every other layer on the deep fetch (Figure~\ref{fig:cpu_tradeoff}),
trading CPU for latency~\cite{bonvoisin2024orm}, a cost a throughput-only
benchmark would miss. That cost is tier-specific: counted at the application
tier alone the efficiency ranking inverts sharply, but counting combined
application-plus-database CPU narrows the gap to about $1.4\times$, since the
native driver's join pushes work onto the database tier that the
application-only figure ignores (Section~\ref{sec:results}). The equal-CPU
control confirms the trade operationally: at an equal one-core budget Prisma
is $4.1\times$ slower than \texttt{pg} and needs about four cores to approach
parity, while MikroORM barely moves with more cores, consistent with a
hydration cost rather than a parallelism-starved one (Section~\ref{sec:results}).

\paragraph{Queueing under load and the open-loop check.}
The pool sampler confirms sustained queueing rather than an idle pool: the
native PostgreSQL driver's ten-connection pool sits fully utilized on the deep
fetch at the 50-connection operating point used throughout
(Section~\ref{sec:methodology}). The open-loop cross-run
(Section~\ref{sec:results}) shows that a closed-loop benchmark alone would
underestimate how abruptly, not just how much, a heavyweight ORM's tail
degrades beyond capacity.

\paragraph{Writes: an engine-bound cost under default durability.}
RQ2 has a clear answer on inserts, measured under each engine's
\emph{default} durability configuration (Section~\ref{sec:methodology}):
PostgreSQL's insert throughput remains layer-dependent even with fsync on,
while MySQL's is governed by an engine-level commit floor common to every
layer, consistent with the per-commit double flush (redo log, then binlog)
InnoDB performs on every insert regardless of how the request reaches
it~\cite{harizopoulos2008oltp}; relaxing durability does not change this
picture (Section~\ref{sec:results}). This warrants caution: a write-throughput
comparison run on a single engine invites the wrong generalization in either
direction (``the access layer is irrelevant to writes'' from MySQL alone, or
``matters a great deal'' from PostgreSQL alone), and an engine-only benchmark
such as~\cite{salunke2024pgmysql} cannot expose this interaction, since it
omits the access layer entirely. Only the dual-engine design makes the
engine-write-path explanation visible rather than confounded with layer
choice, consistent with earlier quantitative ORM work showing database
technology modulates mapping-strategy cost~\cite{lorenz2017orm}; unlike every
read pattern, the write ranking does not transfer across engines at all
(Section~\ref{sec:results}). The single-row insert is not the only write
shape: a transactional multi-statement write, inserting a post and five
comments in one transaction, spreads the representative layers only
$3.3\times$ (Prisma 2412 down to MikroORM 740~req/s; Supplement Table~S8),
close to the single-row insert and far below the widest read spread, because
the transaction's single commit amortizes the per-commit flush across six
inserts. The layer ordering tracks the single-row insert, except for a
\texttt{pg}/Prisma swap at the top (both within a few percent), so the write
conclusions extend to that transactional pattern for the representative
layers measured.

\paragraph{Methodological lessons: a checklist for access-layer benchmarking.}
Reaching the figures above meant confronting four confounds that would
otherwise have silently corrupted the comparison, and their provenance
differs. One, the aggregation fan-out, is a \emph{correctness} bug that our
automated cross-check caught (the same check, comparing each adapter's output
against a native-driver reference on four of the five endpoints, also caught a
driver result-shape mismatch); the other three return \emph{correct} results
and were therefore invisible to any equality check, found only by performance
analysis. We regard the resulting checklist as reusable:
\begin{itemize}[leftmargin=*,noitemsep,topsep=2pt]
  \item \textbf{Aggregation fan-out.} A naive join between \texttt{posts} and
        \texttt{comments} evaluated before the \texttt{SUM} multiplies each
        post's view count by its number of matching comment rows, inflating
        the aggregate, a correctness bug invisible unless every layer's
        output is checked against a reference, not a performance one.
  \item \textbf{OFFSET vs.\ keyset pagination.} An early, OFFSET-based design
        for the range-scan endpoint collapsed on MySQL at
        realistic offsets, which would have been misread as an access-layer
        weakness rather than what it is: a pagination-strategy cost that
        InnoDB pays more dearly for than PostgreSQL. Rewriting every adapter
        around keyset pagination (\texttt{WHERE id < cursor ORDER BY id DESC
        LIMIT n}) removed the confound.
  \item \textbf{Write-workload contamination.} The insert endpoint grows
        \texttt{posts} on every call; because engines and layers commit at
        different rates, later cells in a run would scan a differently sized
        table depending on run order rather than on layer identity, unless
        benchmark-inserted rows are deleted before every cell.
  \item \textbf{Query-formulation confounds.} A first attempt at the
        aggregation endpoint pre-aggregated \emph{all} of \texttt{comments}
        before filtering to one author, so every request re-scanned the whole
        table regardless of which author was requested, a query-plan
        artifact masquerading as an access-layer effect~\cite{leis2015optimizers},
        fixed by rewriting
        the query as a correlated subquery touching only the target author's
        rows.
\end{itemize}
None of these four is schema-specific; each is a generic failure mode of the
genre, extending the database-benchmarking pitfalls cataloged by Fruth et
al.~\cite{fruth2022tail} and Raasveldt et al.~\cite{raasveldt2018fair} to the
access-layer-comparison sub-genre. Each confound is easy to introduce by
accident and hard to detect (the correctness ones without an automated
cross-check, the performance ones without profiling every cell), and at
least one plausibly explains part of the gap between some vendor-reported
and independently verified figures in this space.

\paragraph{Practical guidance.}
The practical synthesis follows directly from RQ1--RQ3, on the
default-configuration estimand a practitioner faces, and applies to the
benchmarked implementations in this configuration, workload, and host rather
than to access-layer categories in general. Where a service's hot
path is dominated by deep/nested fetches, the access layer is consequential,
and a thin layer (the native driver, a query builder, or a query-engine-based
ORM such as Prisma) is the safer default \emph{for throughput and
response-time p99}: a heavyweight data-mapper ORM concentrates its largest
penalty on that traffic, so weigh a query-engine ORM's parity or lead against
its application-tier CPU cost wherever CPU, not throughput, is the
constrained resource. Where the workload is read-simple (point reads, keyset
scans) or write-bound, especially on MySQL, whose engine floor for
single-row inserts dominates layer differences under default durability, a
full ORM's productivity and type safety were not measured here; on throughput
alone the cost is small in these read-simple patterns, so those unmeasured
considerations may reasonably decide the choice on their own. A pragmatic middle path, already
forced onto every layer by our aggregation endpoint, is to keep the ORM for the bulk
of the application and fall back to raw SQL or the native driver only for
endpoints that assemble large object graphs; the same-SQL control shows
this fallback recovers most of the deep fetch's cost (Section~\ref{sec:results}),
and, orthogonally, second-level caching can absorb part of an ORM's read cost
where the workload tolerates staleness~\cite{chen2016cacheoptimizer}.

These are performance findings only: the access-layer decision also turns on
developer productivity, type safety, correctness, maintainability, and
security, none of which this study measures, and which may outweigh the
throughput differences reported here. Even as performance guidance, the
recommendations rest on one schema, one hardware configuration, a
single-host local-database deployment measured under each engine's default
durability for writes, and the pinned library versions; the same-SQL
comparisons bound that cost, not a license to replace idiomatic defaults
with raw SQL in practice. A production deployment adds network latency, TLS,
a reverse proxy, a separate database host, connection poolers, read
replicas, and larger or varied payloads, any of which can shift the balance;
Section~\ref{sec:threats} sets out how far these results can generalize.
