% auto-generated by scripts/gen-tables.mjs — equal-CPU-slice control
\begin{table}[htbp]
  \centering
  \caption{Exploratory equal-CPU sensitivity check (deep-fetch throughput, req/s,
    PostgreSQL, median of 3 runs, three representative layers) with the
    server process confined by \texttt{taskset} to 1, 2, or 4 cores (database
    and load generator pinned to disjoint cores). This bounded sensitivity exposes whether the displayed deep-fetch ordering changes when
    the application process receives the same 1-, 2-, or 4-core budget. Database and
    generator resources remain shared-host controls, not a cross-machine comparison.}
  \label{tab:equalcpu}
  \begin{tabular}{l r r r}
    \toprule
    Layer & 1 core & 2 cores & 4 cores \\
    \midrule
    \texttt{pg} & 3689 & 3688 & 3687 \\
    \texttt{prisma} & 1079 & 1052 & 1219 \\
    \texttt{mikroorm} & 446 & 495 & 522 \\
    \bottomrule
  \end{tabular}
\end{table}
