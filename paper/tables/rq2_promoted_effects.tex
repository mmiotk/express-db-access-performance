% auto-generated by scripts/gen-rq2-leave-prisma-out.mjs
\begin{table}[htbp]
  \centering
  \caption{Effect sizes for the 4 promoted cross-stack reversals. Each row gives the
    paired throughput ratio of the first layer over the second, with its within-campaign
    95\% paired bootstrap interval, on each stack. A pair is promoted only when the two
    stacks place it on opposite sides of parity, both intervals exclude $1.0$, and the
    reversal recurs in the validation campaign. Every promoted pair involves Prisma, whose
    MySQL path uses the MariaDB adapter rather than \texttt{mysql2}, so the design cannot
    attribute these reversals to the DBMS rather than to the adapter, the driver, or
    Prisma's own implementation. The intervals are within-campaign and do not cover
    cross-host or cross-version variation.}
  \label{tab:rq2_promoted_effects}
  \begin{adjustbox}{max width=\textwidth}
  \begin{tabular}{l l r l r l}
    \toprule
    & & \multicolumn{2}{c}{PostgreSQL} & \multicolumn{2}{c}{MySQL} \\
    \cmidrule(lr){3-4}\cmidrule(lr){5-6}
    Pattern & Pair (A vs B) & A$/$B & 95\% CI & A$/$B & 95\% CI \\
    \midrule
    Deep fetch & \texttt{prisma} vs \texttt{objection} & 1.155 & [1.137, 1.164] & 0.754 & [0.743, 0.765] \\
    Deep fetch & \texttt{prisma} vs \texttt{sequelize} & 1.201 & [1.185, 1.208] & 0.716 & [0.710, 0.725] \\
    Aggregation & \texttt{prisma} vs \texttt{objection} & 1.163 & [1.140, 1.176] & 0.806 & [0.798, 0.818] \\
    Insert & \texttt{prisma} vs \texttt{mikroorm} & 1.423 & [1.405, 1.466] & 0.723 & [0.682, 0.804] \\
    \bottomrule
  \end{tabular}
  \end{adjustbox}
\end{table}
