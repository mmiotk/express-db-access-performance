% auto-generated by scripts/pinned.mjs — throughput with DB/server/generator pinned to disjoint cores
\begin{table}[htbp]
  \centering
  \caption{Throughput (req/s) on PostgreSQL with the database (cores 0-3), application server (cores 4-7), and load generator (cores 8-11) pinned to disjoint core sets, approximating the resource separation of a multi-host deployment. The layer ordering is preserved, so it does not depend on the application server having the whole host.}
  \label{tab:pinned}
  \begin{tabular}{l r r}
    \toprule
    Layer & Deep fetch & Point read \\
    \midrule
    \texttt{pg} & 3415 & 6838 \\
    \texttt{prisma} & 2963 & 6109 \\
    \texttt{knex} & 2080 & 4321 \\
    \texttt{mikroorm} & 514 & 1931 \\
    \bottomrule
  \end{tabular}
\end{table}
