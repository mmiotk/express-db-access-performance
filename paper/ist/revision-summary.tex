\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amssymb,amsmath}
\usepackage[protrusion=true,expansion=false]{microtype}
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.5em}
\usepackage{parskip}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=blue}
\newcommand{\euro}{EUR}
\pagestyle{plain}
\begin{document}
\section*{Summary of Revisions}

\textbf{Manuscript:} A Comparability Protocol for Benchmarking Relational Database Access Layers in Express.js \textbf{Journal:} Information and Software Technology \textbf{Revision date:} 26 July 2026

\section*{Semantic admission}

\begin{itemize}
  \item Added an expected-result oracle derived from the deterministic seed and fan-out specification, independent of all adapters and both DBMSs.
  \item Kept differential comparison as a separate equivalence layer and renamed the protocol stage accordingly.
  \item Added exact campaign-state and allocator preflight checks.
  \item Added a 662-row cross-engine seed-parity gate; it caught differing fan-out comment-author assignments hidden by equal counts, and the affected pilot was rejected before the campaign restarted from repetition 1.
  \item Corrected a MySQL reset defect discovered by the new oracle and remeasured all affected patterns.
\end{itemize}

\section*{Treatment and implementation validity}

\begin{itemize}
  \item Renamed the case-study estimand ``policy-selected documented configuration'' and removed the behavioral developer persona.
  \item Made the selection policy substitutable at the protocol level.
  \item Preserved exact documentation responses, timestamps, hashes, conflict resolution, and alternatives.
  \item Added implementation-review provenance as recommended stage R7 and result-blind review packets.
  \item Marked independent human selection and adapter review as pending rather than claiming agreement.
  \item Recorded a result-blind author self-audit covering every adapter. It caught and removed per-insert Knex/MySQL warning I/O and an unused per-request MikroORM aggregation accessor; every affected pilot was preserved and rejected, the full admission chain was rerun, and measurement restarted from repetition 1. This is not counted as independent R7 evidence.
\end{itemize}

\section*{RQ2 and new measurements}

\begin{itemize}
  \item Ran one coherent corrected-state primary campaign covering all 18 compatible treatment--backend pairs for all five access patterns: 90 complete cells x 25 runs, zero timed request failures, and pre/post state checks.
  \item Ran a second independently ordered same-host campaign on 7 portable layers x 2 stacks x 3 prioritized patterns: 42 complete cells x 25 runs, zero timed failures. Deep-fetch ranks reproduce exactly; MySQL insert reproduces only 3/7 ranks and is reported as unstable.
  \item Predeclared persistence criteria retain four material cross-stack reversals and classify all close/nonrecurring changes as exploratory.
  \item Defined RQ2 as backend-stack transfer within the tested same-host campaigns.
  \item Removed causal DBMS attribution and made close rank reversals exploratory.
  \item Retained the explicit limitation that the second campaign is same-host, not cross-host replication.
\end{itemize}

\section*{Protocol contribution}

\begin{itemize}
  \item Narrowed novelty to a concrete domain-specific operational discipline built from established controls.
  \item Cited and distinguished CYNTHIA and retained the direct prior-art boundary.
  \item Added the 2023 Node-Bench comparison as a direct coverage precedent and a ninth source in the protocol audit.
  \item Added a seven-stage, nine-source retrospective protocol audit with 63 source-located judgments and a machine validator.
  \item Described that audit as a one-rater bounded applicability demonstration, not independent validation.
  \item Removed universal completeness and necessity language; each level now states the interpretation it licenses.
\end{itemize}

\section*{Fresh secondary analyses}

\begin{itemize}
  \item Reran common-SQL (16 cells x 10), all-pattern capacity (720 points), matched-utilization (64 cells x 5), longer-tail (18 cells x 10 x 60 s), relaxed durability (18 cells x 5), wait-event, and canonical-constructor analyses with zero accepted request failures.
  \item Updated all numerical claims: selected deep-fetch spread 7.01x/5.12x; common-SQL 1.71x/2.03x; stable 50\% p99 bands 2.3--4.9/1.9--5.5 ms.
  \item Reframed concurrent wait sums and durability changes as compound sensitivity evidence, not a DBMS floor or causal share.
\end{itemize}

\section*{Reproducibility}

\begin{itemize}
  \item Reconciled the main paper with Supplement S31.
  \item Completed an isolated current-candidate reconstruction: 60/60 JSON hashes verified, and 35 table outputs plus four derived analysis JSON files regenerated byte-for-byte without starting either DBMS.
  \item Preserved and verified exact 42-file source archives for each accepted campaign, so later harness maintenance cannot overwrite the code provenance of completed measurements.
  \item Fixed the standalone write-admission cleanup to restore post and comment allocators even after failure; the corrected verifier passes all 18 adapter--engine admissions and leaves both campaign states exact.
  \item Named the five presentation-only differences in the historical 45/50 reconstruction.
  \item Separated author-run offline reconstruction, historical archive-isolated reconstruction, current measurement re-execution, and independent reproduction.
  \item Marked the Docker path as workflow reproduction because its durability and topology differ from the reference numerical path.
  \item Updated the manifest so every revised output maps to its exact data and generator.
\end{itemize}

\section*{Presentation}

\begin{itemize}
  \item Replaced ``strategy standardization'' with ``common-SQL raw-path sensitivity contrast.''
  \item Restricted matched-utilization conclusions to stable 50\% and most 70\% cells.
  \item Regenerated the MySQL insert distribution from corrected state.
  \item Standardized ``deep fetch'' and ``single-row insert.''
  \item Consolidated repeated qualifications and moved exploratory interpretation out of the main argument.
\end{itemize}

\end{document}
