# Word-count declaration

Manuscript: *A Comparative Analysis of Relational Database Access-Layer Performance
in Express.js for PostgreSQL and MySQL* (submission build `ist/ist_main.tex`).

Counted with `texcount` on the seven body sections plus the structured abstract,
with each table and figure counted as 200 words per the IST author guidelines.

| Component | Count |
|---|---|
| Body text (7 sections) | 12,884 |
| Structured abstract | 296 |
| Tables and figures in the main text (9 × 200) | 1,800 |
| **Main-text total** | **14,980** |

This is under the journal's 15,000-word limit. The reference list (70 entries) is
provided separately. Sixteen further tables are placed in the numbered online
supplement (`supplement.pdf`, Supplement Tables S1–S16), which is submitted with
the manuscript and archived under the same Zenodo DOI as the code and data. In
this revision the two cheapest read-pattern tables, the aggregation-pattern table,
and the same-SQL control table were moved to the supplement, and the resource
footprint (CPU and memory) and a primary/secondary outcomes table were added to
the main text, so the main-text float count is unchanged at nine while the
reviewer-requested resource and outcome evidence is now in the body.

Highlights (5 bullets, each ≤ 85 characters) are in `highlights.tex`.
