%% Elsevier Highlights (separate file). 3–5 bullets, each <= 85 characters.
\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{enumitem}
\pagestyle{empty}
\begin{document}
\noindent\textbf{Highlights}
\begin{itemize}[leftmargin=1.5em,nosep]
  \item A reusable comparability protocol turns access-layer benchmarks into experiments
  \item Semantic-equivalence gate: byte-identical responses required before any timing
  \item Same-SQL control bounds strategy vs machinery; deep fetch 7.15x narrows to 1.68x
  \item Rankings are configuration-specific and version-sensitive, not a universal order
  \item Open harness + dual-engine case study (PostgreSQL, MySQL) demonstrate the protocol
\end{itemize}
\end{document}
