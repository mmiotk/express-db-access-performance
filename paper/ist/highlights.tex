%% Elsevier Highlights (separate file). 3–5 bullets, each <= 85 characters.
\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{enumitem}
\pagestyle{empty}
\begin{document}
\noindent\textbf{Highlights}
\begin{itemize}[leftmargin=1.5em,nosep]
  \item Nine documentation-selected layers plus tuned baselines on PostgreSQL and MySQL
  \item Deep-fetch spread of 7.15x narrows to 1.68x under a same-SQL control
  \item Rankings are version-sensitive: an earlier five-core Prisma result did not recur
  \item High-load tail-latency gaps largely dissolve at matched utilization
  \item Open harness verifies byte-identical responses across layers before measuring
\end{itemize}
\end{document}
